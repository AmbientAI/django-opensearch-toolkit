"""Configuration for the sample_app app."""

from django.apps import AppConfig


class SampleAppConfig(AppConfig):
    """Configuration for the sample_app app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "sample_app"
